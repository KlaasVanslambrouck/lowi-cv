# Kopieer dit bestand naar .env.local en vul de waarden in.
#
# TODO: deze variabelen wijzen later naar de publieke, read-only endpoint van
# nidus-api (voor live stats en de Jarvis-chat) en naar het bestaande Nidus
# Supabase-project (tabel `portfolio_content`, met eigen RLS-policy voor `anon`).
#
# LET OP: dit project bevat NOOIT een Anthropic API-key of een Supabase
# service-role key. Uitsluitend publieke, read-only sleutels.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_NIDUS_API_URL=
